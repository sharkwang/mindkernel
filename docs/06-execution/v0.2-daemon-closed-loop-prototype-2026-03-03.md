# MindKernel v0.2 Daemon 闭环原型（2026-03-03）

## 1. 目标
在 v0.2 路线中形成“可运行闭环原型”：

`events -> daemon(observer) -> candidate enqueue -> scheduler -> reflect worker`

## 2. 本次落地范围

### D1（已完成）
- `tools/daemon/memory_observer_daemon_v0_2.py`
- 能力：poll/tail、pid/lock、checkpoint/recover、graceful shutdown
- 验证：`tools/validation/validate_daemon_skeleton_v0_2.py`

### D2（已完成，原型级）
- `core/event_normalizer_v0_2.py`
- 能力：事件标准化、指纹、分钟桶

### D3（已完成，原型级）
- `core/realtime_memory_candidate_v0_2.py`
- 能力：基于规则的候选提炼、风险分层、reflect_job 入队载荷

### D4（已完成，原型级）
- `memory_observer_daemon_v0_2.py` 内置 batch metrics 落盘：
  - processed/normalized/deduped_events
  - candidates/enqueued/dedup_enqueues
  - throttled/skipped_hwm/errors

## 3. 闭环验证
- 新增：`tools/validation/validate_daemon_closed_loop_v0_2.py`
- 覆盖链路：
  1) daemon 读取会话事件
  2) normalize + dedupe + extract + enqueue
  3) reflect worker run-once 消费队列
  4) 产出 summary artifacts

## 4. 门禁状态
- `release_check_v0_1.py --quick`：18/18 PASS
- `release_check_v0_1.py`（full）：21/21 PASS
- CI workflow 已新增 daemon skeleton + closed-loop 两个验证步骤。

## 5. 当前边界
- 当前提炼逻辑仍为规则原型（非最终策略）。
- 观测指标已落盘，但告警策略尚未独立配置。
- 尚未完成 D5/D6（运行策略与回退、门禁分层与夜间回放）。

## 6. 下一步
1. D5：补 runbook + feature flag + 一键回退流程。
2. D6：门禁分层（quick/full/nightly）与时长优化。
3. 衔接外部依赖熔断降级设计（v0.2主线后半段）。

